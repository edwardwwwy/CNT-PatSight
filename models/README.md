# Local models

Place local GGUF model weights in this directory. Model binaries are ignored by
Git and must not be committed.

The phase-four default is:

```text
models/Qwen3-14B-Q4_K_M.gguf
```

Override the model location with the extraction CLI `--model` option when
needed.
